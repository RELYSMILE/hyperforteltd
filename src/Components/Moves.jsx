

const Moves = ({movies}) => {
  return <div>
    <div>Title: {movies.title}</div>
    <div>Release Date: {movies.release_date}</div>
    <div>Producer: {movies.producer}</div>
  </div>
}

export default Moves